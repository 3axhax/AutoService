# Статус задачи

## Этап 1 — подготовка

Статус: `принят`

- Создана ветка `0001-correct-price-algorithm`.
- Создан каталог задачи.
- Резервная копия локальной БД создана в `DumpDB/` и успешно проверена через `pg_restore --list`.
- Код приложения и исходный Excel пока не изменялись.

## Этап 2 — анализ исходной таблицы

Статус: `принят`

- Прочитан лист `Лист1`, диапазон `A1:F59`, без изменения исходного файла.
- Выделены пять категорий техники и варианты цен через `/`.
- Визуально проверены объединённые диапазоны цен первой таблицы и горячая вулканизация D-6–D-9.
- По уточнению пользователя значения `от …` считаются фиксированными, а шиноремонт — суммой материала и одного выбранного способа работы.
- Создан `price-list.json`: 18 обычных услуг, 27 пластырей и 6 групп расходников.
- Проверка: JSON валиден, исходный файл остался неизменённым.

## Этап 3 — документ тестовых данных

Статус: `принят`

- `test-data.md` переработан в 28 реалистичных наборов заказов.
- В каждом наборе один автомобиль; колёса — от 1 до 8; разных работ — не более 10.
- Проверка: суммы пересчитаны как обычные работы плюс материалы и выбранный способ шиноремонта; добавлены негативные проверки ограничений и двойного начисления ремонта.

## Этап 4 — итоговая проверка

Статус: `принят`

- `git diff --check` пройден.
- В рабочем дереве присутствует только каталог текущей задачи; резервная копия остаётся в игнорируемом `DumpDB/`.
- Документы задачи не содержат паролей, токенов, дампов, выгрузок или логов.

## Этап 5 — фиксация текущей реализации

Статус: `принят`

- Добавлен `current-price-calculation.md` с цепочкой загрузки параметров и прайса, хранением выбранных значений и алгоритмом расчёта на клиенте.
- Зафиксирован повторный серверный расчёт при создании и редактировании заказа.
- Отмечены особенности, которые необходимо учитывать при корректировке алгоритма: отдельные реализации на клиенте и сервере, порядок строк прайса, количественные условия и формат скидки.
- Код приложения, API, схема и данные БД не изменялись.

## Этап 6 — общий модуль расчёта

Статус: `принят`

- Выбрано решение: локальный пакет `packages/pricing` с чистой функцией расчёта, используемой клиентом и сервером.
- Ранее созданная резервная копия задачи `DumpDB/correct-price-algorithm-before.dump` повторно проверена через `pg_restore --list`.
- Новая полная выгрузка не создавалась: отдельного разрешения на экспорт операционных таблиц нет; изменения БД в этом этапе отсутствуют.
- Общий пакет подключён как локальная зависимость `@autoservice/pricing`; дублирующий алгоритм удалён из Redux-селектора и `PriceService`.
- Добавлены четыре regression-теста: обычное условие, количество, выборочное влияние скидки и замена менее специфичной строки.
- `packages/pricing`: сборка и 4 теста пройдены.
- `server`: `npm run build` пройден на хосте и в контейнере; Jest завершён с `--passWithNoTests`, поскольку серверных `*.spec.ts` в проекте нет.
- `client`: `npm run build` пройден на хосте и в контейнере; сохранены два существующих предупреждения CSS и предупреждение о размере bundle.
- Smoke check: клиент доступен внутри контейнера; защищённый `GET /price` без авторизации возвращает ожидаемый `403`.
- `docker compose config --quiet` пройден. Полная пересборка образов остановлена на недоступной проверке базового образа Docker Hub до выполнения шагов проекта; Dockerfile разобраны BuildKit без синтаксических ошибок.
- `git diff --check` пройден.

## Этап 7 — аудит достаточности параметров БД

Статус: `принят`

- 2026-09-02 выполнен read-only аудит справочных таблиц компании 3 и их сопоставление с T01–T28; операционные и пользовательские данные не читались.
- Результаты зафиксированы в `database-test-coverage.md`.
- Выявлено отсутствие цен материалов и вулканизации, недоступность горячей вулканизации через зависимость формы, отсутствие «Ножки Г18» в опциях компании и невозможность связать способ с конкретным материалом в плоской структуре заказа.
- По уточнению пользователя лимиты 8 колёс и 10 работ исключены из негативных требований: это только рамки компактности тестовых сценариев.
- Зафиксировано правило: одна ремонтная операция содержит отдельную позицию материала и ровно одну отдельную позицию работы — холодную либо горячую вулканизацию. Объединение материала и работы в одну позицию запрещено.
- Код приложения и данные БД не изменялись.

## Этап 8 — подготовка покрытия T01–T28

Статус: `принят`

- 2026-09-02 получено разрешение выполнить операции, необходимые для тестов.
- Полный новый дамп отклонён из-за риска копирования операционных и пользовательских данных.
- Ранее созданный дамп задачи повторно проверен; дополнительно создан и проверен дамп только пяти затрагиваемых справочных таблиц. Заказы, смены, сессии и пользователи в него не вошли.
- Миграция добавила nullable-поле `compositeOperationId`, опции 225–229, доступ к опции 224 и 84 строки цен/условий компании 3. Повторный запуск не создал дубликатов.
- Общий расчёт использует количество основной опции; ремонты рассчитываются как связанные, но раздельные позиции материала и работы.
- В форму добавлен список шиноремонтов; недоступный для материала способ скрывается, а сервер повторно проверяет пару.
- Шиноремонт переведён в отдельный параметр `tire_repair` типа `COMPOSITE_LIST`. Состав групп, русские подписи и их порядок хранятся в `orderParametersOptions`; UI, серверная проверка и расчёт не привязаны к имени `materials` или диапазонам ID.
- Порядок `tire_repair = 10` размещает карточку после `materials = 9` данными справочника, без ручной вставки JSX.
- Выбор материала и способа работы в составной карточке переведён на общий `SelectUI`: оформление совпадает с другими селекторами приложения, доступен поиск по вариантам.
- Перед миграцией создан и проверен ограниченный дамп справочников `DumpDB/tire-repair-parameter-reference-before-20260902.dump`.
- Миграция и исправляющая миграция имени колонки повторно запущены: каждая метка существует в одном экземпляре, порядок параметров и 84 строки прайса не изменились.
- Проверки: T01–T28 и общие regression-тесты — 33/33; server Jest — 2/2; server/client build на хосте и в контейнерах — успешно; целевой ESLint — успешно; авторизованный API smoke вернул `tire_repair` с группами 27 материалов и 2 способов сразу после `materials`.

## Этап 9 — итоговая компоновка и фиксация реализации

Статус: `принят`

- 2026-09-02 по результату проверки интерфейса отменены изменения, переводившие карточки составных работ в горизонтальный ряд и динамически расширявшие карусель.
- Несколько шиноремонтных работ снова выводятся вертикально; группы выбора и количество внутри каждой работы также расположены сверху вниз.
- Общие поисковые селекторы `SelectUI`, конфигурируемый тип `COMPOSITE_LIST` и размещение параметра после материалов через справочник сохранены.
- После отката компоновки целевой ESLint и production-сборка клиента прошли успешно; контейнер клиента пересобран и работает.
- Реализация расчёта, миграций, составного параметра, тестов и документации зафиксирована коммитом `083e0ef` (`feat: add configurable composite service works`).

## Этап 10 — модальное добавление и редактирование составной работы

Статус: `принят`

- 2026-09-03 подтверждено, что запрос является продолжением существующей задачи 0001; отдельный каталог задачи не создаётся.
- Перед изменениями создана и проверена через `pg_restore --list` резервная копия локальной БД `DumpDB/composite-parameter-modal-before-20260903.dump`; содержимое дампа не выводилось.
- Зафиксирован текущий интерфейс: поля составной работы редактируются непосредственно внутри карточки карусели, а кнопка «Добавить» сразу записывает операцию с автоматически выбранными значениями.
- Принято решение использовать существующий общий компонент `Modal`: одна локальная черновая операция обслуживает и добавление, и редактирование; Redux обновляется только после подтверждения.
- Кнопка «Добавить» теперь открывает модальную форму с группами составного параметра и количеством; Redux обновляется только по «Применить».
- Для существующей записи добавлено действие редактирования, открывающее ту же модалку с копией текущих значений. Отмена, закрытие по фону и Escape очищают черновик без изменения заказа.
- В карусели поля ввода заменены кратким составом каждой записи, количеством и отдельными действиями редактирования/удаления.
- Целевой ESLint пройден без предупреждений. Production-сборка клиента пройдена на хосте и в контейнере; сохранились ранее известные предупреждения CSS и размера bundle, а на хосте также предупреждение о версии Node.
- Все три контейнера запущены, PostgreSQL имеет статус `healthy`.
- Ручной smoke в Chrome не выполнен: FSB не подключён к текущему окружению. Этап оставлен `на проверке` до визуальной приёмки добавления, отмены, редактирования и удаления.
- 2026-09-03 пользователь уточнил дефект: после «Применить» цена меняется корректно, но во время работы с модалкой нет живого пересчёта. Автоматический компонентный тест невозможен без отсутствующей в клиенте тестовой инфраструктуры; воспроизведение зафиксировано по наблюдаемому сценарию.
- Перед исправлением создана и проверена резервная копия `DumpDB/composite-price-fix-before-20260903.dump`.
- В модалку добавлен предварительный итог всего заказа. Он рассчитывается общим `calculateOrderPrice` из текущего заказа и локальной черновой операции, обновляется при выборе опций и количества и не меняет Redux до подтверждения.
- После исправления: общий набор pricing-тестов — 33/33; целевой ESLint и Prettier — успешно; production-сборка клиента на хосте и в контейнере — успешно. Все три контейнера запущены, PostgreSQL `healthy`.
- 2026-09-03 по результату проверки пользователь отклонил вывод суммы внутри модалки: предварительный итог удалён полностью.
- Уточнено фактическое воспроизведение: после «Применить» карточка шиноремонта появляется, но основной итог заказа остаётся `0 ₽`. Чистый `calculateOrderPrice` для показанных опций 185 и 179 возвращает ожидаемые `1250 ₽`, а соответствующие строки локального прайса существуют.
- Причина локализована в клиентском адаптере суммы: перед общим калькулятором состояние заказа проходило лишнюю фильтрацию. `orderTotalValue` теперь передаёт полное состояние заказа в `calculateOrderPrice`, который самостоятельно исключает служебные поля и параметры без опций.
- Добавлен regression-тест полного состояния нового клиентского заказа (`id`, `active`, составная операция) с ожидаемой суммой `1250 ₽`.
- Последующая проверка клиентского селектора опровергла первоначальную гипотезу: и с фильтром он возвращает ожидаемые `1250 ₽`, поэтому изменение селектора отменено.
- Фактическая причина подтверждена в работающем контейнере: Vite обслуживал сохранённую оптимизированную копию `@autoservice/pricing` со старым алгоритмом без `COMPOSITE_LIST`, хотя актуальный `packages/pricing/dist` уже содержит составной расчёт.
- В `vite.config.ts` включена принудительная переоптимизация зависимостей при запуске dev-сервера, чтобы локальный общий пакет не оставался устаревшим после пересборки образа или перезапуска контейнера.
- Клиентский контейнер перезапущен; проверено, что новая оптимизированная копия содержит обработку `compositeParameterNames`. Сумма в модалке отсутствует.
- Проверки после окончательного исправления: pricing-тесты — 34/34, целевой ESLint и Prettier — успешно, production-сборка клиента на хосте — успешно.
- 2026-09-03 пользователь завершил приёмку интерфейса по фактической работе приложения: модальное добавление/редактирование принято, вывод суммы из модалки удалён, основной итог использует актуальный составной расчёт.
- Этап и задача приняты; незавершённых изменений API, схемы или данных для этого этапа нет.
- Финальная проверка перед слиянием: pricing 34/34, server 2/2, server/client production build, целевой client ESLint, `docker compose config --quiet` и `git diff --check` — успешно. Сохранились только ранее известные предупреждения клиентской сборки о CSS, размере bundle и версии Node на хосте.
